[project]
name = "medical-demo-backend"
version = "0.1.0"
description = "Safety-gated medical demo backend for the Flutter leishmaniasis app."
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
