# patching_retriever

Organized fixes and tests for the RAG retriever.

- `fixes/`   : Python modules that patch/improve retriever behavior  
- `tests/`   : Validation and debugging scripts (runnable directly or with pytest)  
- `scripts/` : Shell helpers for restarting containers, batch tests, etc.
